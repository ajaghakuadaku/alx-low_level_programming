#include <stdio.h>

/**
* main - program that prints the alphabet in lowercase
* Return: Always (Success)
*/
int main(void)
{
	char alphb;

	for (alphb = 'a'; alphb <= 'z'; alphb++)
	putchar(alphb);
	putchar('\n');
	return (0);
}

