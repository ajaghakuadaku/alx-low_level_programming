#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @c: value to be checked
 * Return: 1 if uppercase and 0 for any other
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
	return (1);
}
{
	return (0);
}
}
