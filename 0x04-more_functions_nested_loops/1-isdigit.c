#include "main.h"

/**
 * _isdigit - checks for a digit
 * @c: character to be checked
 * Return: success 1 but 0 if otherwise'
 */

int _isdigit(int c)
{

	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	{
		return (0);
	}
}
